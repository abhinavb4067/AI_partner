AI Girlfriend App — Work Log
Date: 2026-09-11

=== WHAT WAS DONE TODAY ===
Reviewed the new Flutter mobile app port at mobile_app/ (currently untracked in git,
never committed). It mirrors the web app: auth, AI chat, human-to-human chat + WebRTC
calls, pricing/payment webview, social/discover/matching, E2E crypto (X25519/libsodium,
ported from the web app's tweetnacl utils/crypto.js).

Files reviewed (code-review pass only — no Flutter SDK installed on this machine, so
`flutter analyze` / a real build was NOT run):
  - mobile_app/lib/core/api_client.dart      -> OK, no issues
  - mobile_app/lib/core/session.dart         -> OK, no issues
  - mobile_app/lib/core/crypto_service.dart  -> OK, no issues
  - mobile_app/lib/core/e2e_crypto.dart      -> OK, no issues
  - mobile_app/lib/core/ws_service.dart      -> OK, no issues
  - mobile_app/lib/services/chat_service.dart   -> OK
  - mobile_app/lib/services/call_manager.dart   -> OK (good session-guarding on async WebRTC callbacks)
  - mobile_app/lib/services/payment_service.dart -> only has getPlans(), no checkout call — needs confirming (see below)
  - mobile_app/lib/screens/chat/chat_screen.dart -> BUG FOUND (see below)

=== PROGRESS STATUS ===
- mobile_app/ is functionally complete-looking (pubspec.yaml/.lock present, lib/ fully
  built out, test/ folder exists) but has NEVER been committed to git. It's still fully
  untracked.
- .gitignore inside mobile_app/ is already correct (excludes build/, android/, ios/,
  macos/, windows/, linux/, web/, .dart_tool/, etc.) — running `git add mobile_app`
  would stage only ~47 real source/config files, not the 2.2GB build/ folder.

=== BUG FOUND (NOT YET FIXED) ===
File: mobile_app/lib/screens/chat/chat_screen.dart, function _loadHistory()
Problem: `_error` is set when the history fetch fails, but is never cleared back to
null on a subsequent successful/retried load. There is also no retry button anywhere
in the UI. Net effect: if the very first history load fails (e.g. flaky network on
cold start), the chat screen gets stuck showing the error message permanently with no
way to recover except leaving and re-entering the screen. This is the closest match
in the code to a "chat screen goes blank / stuck" symptom.
Fix needed:
  1. Reset `_error = null` at the top of `_loadHistory()` before the try block.
  2. Add a retry button/action in the error-state UI (around line 228-229) that calls
     `_loadHistory()` again.

=== REMAINING / NEXT STEPS ===
1. Fix the chat_screen.dart bug above.
2. Confirm payment_service.dart is intentionally thin — check pricing_screen.dart and
   payment_webview_screen.dart to verify the payment flow goes through the in-app
   WebView to the web app's checkout (per env.dart's comment about WEB_APP_URL) rather
   than needing a native subscribe/checkout API call. Was about to check this when work
   paused today.
3. Commit mobile_app/ to git once reviewed (git add mobile_app -> ~47 files, build/
   and platform dirs correctly ignored).
4. Install Flutter SDK on this machine (or verify elsewhere) and run `flutter pub get`
   + `flutter analyze` to catch compile errors — never actually verified this builds.
5. Still not reviewed: auth screens (login/register/forgot/reset), call_screen.dart,
   social/discover/matches/human_chat screens, profile_screen.dart, push_service.dart,
   voice_service.dart, google_auth_service.dart, router.dart, main.dart, theme.dart,
   widgets/ (gradient_avatar, incoming_call_overlay).

=== SEPARATE NOTE FROM TODAY ===
Earlier in the session there was a question about "the chat session goes blank" —
this turned out to be about the Claude Code chat panel in the VSCode extension going
blank (likely context summarization or a session/tab reset), NOT an app bug. Nothing
to fix in the codebase for that; if it recurs, check for a session/history picker in
the extension to reopen the prior transcript.


================================================================================
Date: 2026-09-13
================================================================================

=== WHAT WAS DONE TODAY ===
Full security audit of backend/ + fixes for everything found, then a cost/pricing
pass, then a Cloudflare R2 storage migration, then a real NSFW image-gen bug fix.
Everything below is committed to git (main branch) AND already deployed live on
the server (165.22.11.170, /var/www/AI_partner) — this was not just planned, it's
running in production right now.

--- 1. SECURITY FIXES (all deployed) ---
Critical:
  - chat.py: POST /api/chat/ and GET /api/chat/history/{char_id} now require JWT
    auth (Depends(get_current_user)) — previously anyone could chat as/drain
    credits of/trigger paid image-gen for ANY user by just knowing their email,
    since user_id came from the request body, not the token.
  - payment_service.py: removed the "if 'DUMMY' in key: auto-approve" bypass.
    Replaced with explicit PAYMENT_MOCK_MODE setting (default False) + fails
    CLOSED (rejects) when gateway keys are missing/unconfigured.
High:
  - Added slowapi rate limiting: login/register/OTP/reset-password (5/min),
    chat (20/min), chat history (60/min), TTS/transcribe (10/min).
  - /api/payment/verify now has an idempotency guard (can't be credited twice).
  - config.py refuses to boot if CORS_ORIGINS contains "*" (paired with
    allow_credentials=True that's a token-theft vector).
Medium/low:
  - Google-only accounts (empty hashed_password) can no longer be silently
    hijacked by whoever calls /login first with any password.
  - main.py refuses to boot in production with the default admin password.
  - Media (AI-companion chat photos at /media/<char>/<user>/<file>) is now
    auth-gated + ownership-checked via new app/api/routes/media.py — this
    ALSO required an nginx fix (nginx_backend.conf) since nginx was serving
    /media/ directly via `alias`, bypassing FastAPI entirely; added a regex
    location for the 3-segment path shape that proxies to the backend instead.
  - WebSocket auth token moved off the URL path onto the Sec-WebSocket-Protocol
    subprotocol (ws_chat.py) so it stops appearing in proxy logs/browser
    history. Old /{token} route kept one release as a fallback.
  - requirements.txt fully pinned to versions verified via `pip freeze` on the
    actual production venv (not just latest-locally-installed).
  - ADMIN PASSWORD WAS CHANGED on the server — new password is in
    backend/.env on the server (ADMIN_PASSWORD=...) and was shown once to the
    user directly in chat. NOT repeated here since this file is git-tracked.
    ENVIRONMENT=production was also set in .env (arms the boot-guard above).

--- 2. CLOUDFLARE R2 MIGRATION (implemented, bucket created, LIVE) ---
  - New app/services/r2_service.py (S3-compatible client via boto3).
  - image_service.py uploads new chat-generated photos straight to R2 instead
    of local disk; falls back to local disk on upload failure or when
    R2_ENABLED=False. Old local images still work via automatic fallback in
    the media route (checks R2 first via object_exists, falls through).
  - media.py: ownership-checked, then redirects (302) to a 5-min presigned R2
    URL — image bytes never touch the app server's bandwidth once on R2.
  - R2_ENABLED=true is SET on the server right now. Bucket: avoiga-chat-media.
    Account ID + API token credentials are in backend/.env on the server —
    NOT duplicated in this file (it's git-tracked).
  - Verified end-to-end with a real upload/presign/fetch/cleanup test AND a
    real ownership-check test (owner token = 200, wrong user's token = 404).
  - Setup docs for reference: backend/docs/R2_SETUP.md

--- 3. PRICING (implemented, DB updated live, no longer test values) ---
  - Credit costs recalibrated to real API cost ratios: text=1, photo 5->8,
    voice 2->15 (chat.py / voice.py).
  - Real plans now live in the SubscriptionPlan table (were placeholder test
    values like Starter=Rs10/10 credits before):
      Free:    Rs0/mo    — 30 credits
      Starter: Rs149/mo  — 400 credits
      Pro:     Rs399/mo  — 1500 credits
      Elite:   Rs899/mo  — 6000 credits, marketed as "Unlimited" in the UI but
               is_unlimited stays FALSE with a real hard cap enforced
               server-side (true is_unlimited=True bypasses ALL credit
               gating in chat.py — that was the actual cost-abuse risk).
  - Frontend (Pricing.jsx / Profile.jsx / PaymentModal.jsx) shows "Unlimited"
    for Elite by checking plan_name=="elite", not the is_unlimited flag.
  - Fixed stale `features` text array on each plan (was still showing old
    credit numbers even after monthly_credits was updated).
  - Breakeven at current Rs600/mo server + ~Rs85/mo domain (~Rs700/mo fixed
    costs): covered by as few as ~3 paying Starter-tier customers.

--- 4. FRONTEND DEP SECURITY (fixed, deployed) ---
  - Ran `npm audit fix` — resolved all 13 vulnerabilities (1 low/2 moderate/
    10 high), all within existing semver ranges, no package.json version
    bumps needed, build verified clean before+after deploy.

--- 5. BUG FOUND + FIXED: NSFW image generation was completely broken ---
  - Root cause: NSFW/explicit photo requests routed to a local Stable
    Diffusion pipeline (app/services/local_image_gen.py) requiring
    torch+diffusers and pipe.to("cuda") — confirmed NONE of that exists on
    this server (no torch/diffusers installed, no nvidia-smi, no GPU at all).
    Every NSFW photo request was silently failing (caught by try/except,
    returned no image, nothing logged clearly) — likely since this server
    was first set up, since the app's character personas actively push
    toward NSFW content.
  - Fix: NSFW now routes to fal.ai's fast-sdxl (~Rs0.33/photo) instead of the
    local GPU path — same fal.ai account already used for SFW, no new
    credentials needed. Verified live with a real fal.ai call that returned
    an actual image URL (HTTP 200).
  - Local GPU path kept intact behind new setting
    LOCAL_GPU_IMAGE_GEN_ENABLED (default False), for if a GPU server is used
    later.

=== NOT YET DONE / NEXT STEPS FOR TOMORROW ===
1. **Async blocking-call fix (important, not yet done)**: image_service.py
   still uses synchronous `requests.post/get` calls inside async route
   handlers. Since the server runs a SINGLE gunicorn worker (-w 1), any one
   photo-generation request currently freezes the ENTIRE server for every
   other user (logins, chats, everything) for the 5-30s the fal.ai call
   takes. Fix: wrap those calls in `asyncio.to_thread(...)` or switch
   image_service.py to httpx.AsyncClient like ai_service.py already does.
   This matters MORE than the droplet upgrade below — a bigger server alone
   doesn't fix this, it just raises how many simultaneous blocked requests
   before things queue up.
2. **Droplet upgrade decision pending**: current server is 1 vCPU/512MB RAM
   (~Rs600/mo), shared with the unrelated Rent Management app + Postgres.
   Realistic concurrent-user capacity right now: ~5-10 people at the exact
   same moment before things degrade. A $18-24/mo (2 vCPU/4GB) droplet with
   2-4 gunicorn workers would handle ~100-150 concurrent users comfortably —
   but only once item #1 above is also fixed. User was deciding whether/when
   to upgrade; no droplet resize has been done yet.
3. Consider whether to also fix the SFW flux-pro-ultra cost tail-risk
   (₹5/photo) the same way — e.g. defaulting SFW to a cheaper model too, or
   leaving flux-pro since it's higher quality and the worst-case cost is now
   less likely given NSFW (the more common request type per the persona
   prompts) already moved to the cheap fast-sdxl path.
4. Mobile app (mobile_app/) review from 2026-09-11 above is still pending —
   was not touched today, remains a separate open item.
5. Server disk is tight: 8.7GB total, was 78% full before R2 migration.
   Worth checking again now that new photos go to R2 instead of local disk —
   disk pressure should stop growing from that source at least.

=== HOW TO PICK THIS BACK UP ===
Server access: SSH key at "D:\My Projects\server for  ai girl friend\do_ed25519",
root@165.22.11.170. App lives at /var/www/AI_partner (git remote:
github.com/abhinavb4067/AI_partner, branch main — server is in sync with it
as of the end of today's session). Backend service: `systemctl restart
fastapi`. Frontend: `cd frontend && npm run build && systemctl restart nginx`.


================================================================================
Date: 2026-09-14
================================================================================

=== WHAT WAS DONE TODAY ===
Continued from yesterday's "NOT YET DONE" list: fixed the async blocking-call
issue, then went deep on why NSFW image generation still doesn't work even
after yesterday's fal.ai fast-sdxl routing fix. Everything below is committed
to git (main) AND deployed live on the server.

--- 1. ASYNC BLOCKING-CALL FIX (implemented, deployed, verified live) ---
  - chat.py: ImageService.generate_smart_image() (sync requests.post to
    fal.ai + R2 upload + disk I/O) is now called via `await
    asyncio.to_thread(...)` instead of directly — was freezing the ENTIRE
    server (single gunicorn worker, -w 1) for every other user during the
    5-30s a photo request takes.
  - voice.py: /transcribe's blocking ffmpeg subprocess.run() + local Whisper
    CPU inference (STTService.transcribe) factored into a new
    _convert_and_transcribe() helper, also run via asyncio.to_thread.
  - Verified LIVE with a real test: fired a real chat+photo request, hammered
    /api/health in parallel — health checks stayed at a steady ~0.9-1.2s
    throughout the ~4s chat request (no freeze). Confirmed via server logs
    that the image-gen code path was actually exercised during the test.
  - text chat (ai_service.py) and TTS (voice_service.py) were already
    properly async via httpx.AsyncClient — no changes needed there.

--- 2. BONUS FIX: server logs were completely invisible (found while testing #1) ---
  - /etc/systemd/system/fastapi.service had no PYTHONUNBUFFERED=1, so all
    print() output sat in a buffer and never reached `journalctl` — a
    previous test run showed literally zero log lines despite a real,
    successful request completing. Added
    `Environment="PYTHONUNBUFFERED=1"` to the service file directly on the
    server (backed up original to /root/nginx_backups/fastapi.service.bak-*
    first), daemon-reload + restart. Logs work correctly now — this is how
    finding #3 below was even possible to diagnose.

--- 3. NSFW IMAGE GENERATION: root cause found, provider research done, NOT YET FIXED ---
  With logs now working, caught the real failure live: fal.ai's fast-sdxl
  (yesterday's fix) returns HTTP 422 content_policy_violation on NSFW
  prompts. Checked fal.ai's own docs (fal.ai/docs/errors): NSFW is
  EXPLICITLY listed as a policy-violation trigger, marked non-retryable, no
  account setting/parameter/alternate endpoint can disable it. This is a
  hard platform-wide ban, not a bug -- fal.ai categorically cannot be used
  for NSFW generation, full stop.

  User does NOT want fal.ai removed (already paid ~Rs1000 for a
  subscription) -- keep it for SFW image generation only, find a separate
  provider for NSFW.

  Research done (checked each provider's ACTUAL policy docs, not just
  marketing, after confirming fal.ai's marketing was misleading):
    - Novita AI: looked promising (cheap $0.0015/img, true pay-as-you-go,
      "optional NSFW detection" language suggested no blanket ban) BUT their
      Acceptable Use Policy Section 5(7) explicitly states content that "is
      sexually explicit" is prohibited, no carve-out for consenting adults.
      Confirmed dead end via https://novita.ai/legal/acceptable-use-policy
      -- do NOT use for NSFW.
    - ModelsLab (formerly stablediffusionapi.com) -- STRONGEST CANDIDATE:
      their own product docs explicitly document a model_id="nsfw" and a
      "safety_checker": "no" request parameter -- this is a documented,
      marketed feature, not an accidental loophole. Their ToS doesn't ban
      adult content (unlike Novita's explicit ban). Endpoint:
      https://modelslab.com/api/v7/images/text-to-image (or v4/dreambooth
      for the "nsfw" model specifically), key passed in JSON body not header.
      Pricing: NO free trial, Basic plan $21/mo (~Rs1750) for 3,250 calls/mo
      (~Rs0.54/image AT FULL 3250-image usage; effectively a flat ~Rs1750/mo
      cost at current low volume since unused calls likely don't roll over).
      Has a genuine 24-hour full-refund window on NEW subscriptions only
      (not renewals) -- this is the safety net: subscribe, test immediately,
      cancel within 24h for full refund if it doesn't actually work.
      One unrelated Trustpilot complaint found about general output
      relevance/quality ("asked for a house, got a horse") -- not NSFW
      specific, just a quality flag to keep in mind.

  DECISION NOT YET MADE: user was weighing whether the effective ~Rs1750/mo
  flat cost (at current low volume, well under 3,250 images/mo) is worth it
  right now vs waiting. Nothing purchased, nothing implemented yet.

--- 4. FIXED: credits were charged even when photo generation failed ---
  - chat.py: credit deduction moved from "immediately on request" to "after
    we know the real outcome" -- the affordability check (can they afford
    it) still happens upfront, but the actual deduction now happens after
    generation completes. Photo requested + delivered = full photo rate
    (8cr). Photo requested but generation failed (e.g. the fal.ai 422 above)
    = charged the cheaper text-only rate (1cr) instead, since a text reply
    was still delivered either way.
  - Verified live: confirmed via server logs that both of two real test
    requests correctly logged "charging text-only rate (1cr) instead of
    photo rate (8cr)" after a failed NSFW generation attempt.

=== NOT YET DONE / NEXT STEPS FOR TOMORROW ===
1. **Decide on ModelsLab subscription** -- if going ahead: sign up, get API
   key, IMMEDIATELY test a real NSFW request before the 24h refund window
   closes, then implement the actual integration (new
   app/services/modelslab_service.py or similar, wire into
   image_service.py's NSFW branch alongside the existing fal.ai SFW path).
   fal.ai credentials/subscription stay untouched -- keep using for SFW.
2. Droplet upgrade decision still pending from yesterday (1 vCPU/512MB,
   ~Rs600/mo currently -> ~Rs1800-2000/mo for 2vCPU/4GB would handle
   ~100-150 concurrent users, now that the async fix from today is also in
   place to actually take advantage of it).
3. Consider the SFW flux-pro-ultra cost tail-risk (Rs5/photo) -- still not
   addressed, mentioned twice now, still open.
4. Mobile app (mobile_app/) review from 2026-09-11 still pending, untouched.
5. Disk space check now that R2 + today's fixes are live -- was 78% full
   before R2 migration, worth a fresh look.

=== HOW TO PICK THIS BACK UP (still accurate) ===
Server access: SSH key at "D:\My Projects\server for  ai girl friend\do_ed25519",
root@165.22.11.170. App lives at /var/www/AI_partner (git remote:
github.com/abhinavb4067/AI_partner, branch main). Backend: `systemctl restart
fastapi`. Frontend: `cd frontend && npm run build && systemctl restart nginx`.
Note: fastapi.service now has PYTHONUNBUFFERED=1 -- logs work, use
`journalctl -u fastapi --since '<time>' --until '<time>'` to debug.


================================================================================
Date: 2026-09-14 (continued -- same-day extended session, weekly limit burn)
================================================================================

User asked to keep working autonomously overnight until the weekly Claude
limit resets (~7:29am), no need to ask permission for changes within this
repo + the server (explicitly the only two things in scope -- no new
external signups/purchases without asking first). Two things done:

--- 1. FINISHED the mobile app code review from 2026-09-11 ---
Reviewed every remaining unreviewed mobile_app/lib file (auth screens,
call_screen, social screens, profile_screen, push/voice/google-auth/social/
profile/payment services, router, main, theme, widgets). Found and FIXED:

  - mobile_app: ChatService.getHistory() was still calling the OLD
    /api/chat/history/{user_id}/{char_id} URL -- backend dropped that path
    segment on 2026-09-13 (IDOR fix). Every mobile history load would have
    404'd. Fixed to /api/chat/history/{char_id}.
  - mobile_app: WsService was still connecting via the deprecated
    /api/ws/chat/{token} URL-path-auth route. Switched to /connect +
    WebSocket subprotocol (IOWebSocketChannel.connect(uri, protocols:
    [token])), matching the fix already made web-side on 2026-09-13.
  - **frontend (web) -- real, currently-live bug found**: Profile.jsx's
    "Log Out" button cleared a non-existent 'user_token' localStorage key
    instead of the actual 'token' key that gates auth (ProtectedRoute.jsx
    checks localStorage 'token'). Clicking Log Out NEVER actually logged
    the user out -- session silently survived. This was live in production
    before today. Fixed by centralizing logout into a new exported
    clearSession() in api.js, used by every logout path (manual button,
    forced 401 handler, multi-tab SESSION_TERMINATED sync) so the key list
    can't drift out of sync again like this.
  - mobile_app: GoogleAuthService.signOut() was never called anywhere --
    Google-signed-in users' native session survived app logout (account
    picker would silently re-auth same account). profile_screen.dart now
    calls it best-effort alongside Session.logout().
  - Backend: added DELETE /api/profile/fcm-token (clears fcm_token) as the
    foundation for fixing "push token never cleared on logout" on either
    platform -- NOT YET wired into either client's actual logout flow, that
    part is still a follow-up.
  - mobile_app: undisposed TextEditingControllers fixed in
    human_chat_screen.dart (+ScrollController) and all 4 auth screens
    (register/login/reset-password/forgot-password) -- resource leaks.
  - No bugs found in call_screen.dart (self-recovers via endCall() -> pop,
    doesn't have chat_screen.dart's old "stuck error" pattern) or in
    social_service.dart / social screens (no IDOR pattern -- identity always
    via bearer token, never a client-supplied user_id).
  - Confirmed (was previously unconfirmed): payment_webview_screen.dart
    does go through an in-app WebView to the web app's own checkout, NOT a
    native purchase API (none exists) -- but the WebView's
    ?mobile_token=&mobile_uid= auto-login bootstrap has NO corresponding
    support in frontend/src (grep confirms zero matches) -- so today the
    WebView loads logged-out and the user has to log in a second time
    inside it. Flagged, NOT fixed (would need a frontend entry-point change
    to seed localStorage from those query params) -- follow-up.

  mobile_app/ was already committed to git in a prior session (not by me
  today) -- today's changes are normal commits on top of that, not a first
  commit.

--- 2. NEW FEATURE: "Our Story" relationship timeline (web + mobile) ---
Built on the user's explicit instruction to add something genuinely
differentiated/"never exist in world apps" while working autonomously --
most AI-companion apps only ever show the raw chat log. This turns data the
app was ALREADY storing (ChatMessage timestamps, UserMemory facts) into a
living relationship timeline: a day counter, a growing "relationship stage"
(Just Met -> Getting Curious -> Growing Close -> Inseparable -> Deeply
Bonded -> Soulmates -> Forever, at 0/3/7/14/30/60/180 days), milestone
cards (first message, first photo, message-count badges at 10/50/100/500/
1000/5000), and a visible "what she remembers about you" list. Zero new
paid API, zero new recurring cost -- pure presentation layer over existing
data, which is why it was safe/cheap to ship in one session.

Product intent (recorded so it isn't lost): this is a deliberate
loss-aversion / attachment hook -- once someone has a real streak and a
page full of milestones, letting a subscription lapse feels like losing
something, which a bare chat log never creates. This is the retention lever
it's meant to pull; it is NOT currently gated behind any paid plan (works
for Free-tier users too) -- whether to gate deeper story features (e.g.
only paid plans keep story history past N days, or unlock a "look back"
export) behind subscription tiers is an open product decision, not decided
tonight.

Implementation:
  - Backend: GET /api/chat/story/{char_id} (app/api/routes/chat.py, rate
    limited 30/min) -- computes everything from existing ChatMessage +
    UserMemory rows, no schema changes needed.
  - Web: new page at /story/:charId (frontend/src/pages/Story/Story.jsx),
    entry point is a small book-emoji button in the chat header
    (Chat.jsx).
  - Mobile: new StoryScreen (mobile_app/lib/screens/chat/story_screen.dart)
    mirroring the web page, entry point in chat_screen.dart's AppBar
    actions, ChatService.getStory() + RelationshipStory/RelationshipStage/
    StoryMilestone/StoryMemory models in chat_service.dart.

Verified LIVE on the actual server with real user data (not just code
review): GET /api/chat/story/<real char id> with a real token returned a
real computed result -- 35 days together, stage "Deeply Bonded", 2 real
milestones, 2 real memories pulled from the DB. Web page route
(/story/<charId>) confirmed returning HTTP 200. Backend restarted clean,
frontend rebuilt and redeployed, nginx restarted, both services active
after.

All of today's second-session work (backend chat.py/profile.py, frontend
App.jsx/api.js/Chat.jsx/Profile.jsx/Story.jsx, mobile_app across 12 files)
is committed to git (main, commit 73eefb3) AND deployed live on the server
-- nothing is sitting uncommitted or undeployed.

=== STILL OPEN AFTER TONIGHT (carried forward + new) ===
1. ModelsLab subscription decision (from earlier today) -- still not
   decided/purchased.
2. Droplet upgrade decision -- still pending.
3. SFW flux-pro-ultra cost tail-risk -- still not addressed.
4. FCM token cleanup -- backend endpoint exists (DELETE /api/profile/
   fcm-token) but NEITHER client (web nor mobile) actually calls it on
   logout yet. Small follow-up: call it from Session.logout() equivalents.
5. payment_webview_screen.dart's mobile_token/mobile_uid auto-login bridge
   has no frontend support -- mobile checkout works but requires a second
   manual login inside the WebView. Needs a small frontend entry-point
   change to read those query params and seed localStorage, OR remove the
   dead query params and document that manual WebView login is expected.
6. "Our Story" feature is unguarded/ungated -- whether to tie any part of
   it to subscription tier is an open product decision, not made tonight.
7. Flutter SDK still not installed anywhere this has been tested from --
   all mobile_app review/fixes tonight were manual reads + the IDE's Dart
   analyzer (via VSCode diagnostics), never an actual `flutter analyze` or
   build. Real build verification is still outstanding.

--- ITEMS 4 AND 5 ABOVE: DONE (later in the same overnight session) ---
4. FCM token cleanup -- DONE. api.js's clearSession() and mobile's
   profile_screen.dart _logout() both now call DELETE /api/profile/
   fcm-token before clearing the session (best-effort, never blocks
   logout). Verified live against the real endpoint.
5. payment_webview_screen.dart's mobile_token/mobile_uid bridge -- DONE.
   Implemented in frontend/src/main.jsx (has to run before React's first
   render, not in a useEffect -- Pricing.jsx reads localStorage
   synchronously during render for isLoggedIn, so a post-mount effect
   wouldn't reach it in time without a reload). Updated the Dart screen's
   doc comment to match. Verified the route still loads with the query
   params present.

--- ALSO DONE (extra safety item, not on any prior list) ---
- Added a hard daily cap (30/user/day) on fal.ai photo generations,
  independent of credits/plan. This is the mitigation for the flux-pro-
  ultra cost tail-risk flagged twice earlier today (item 3 above still
  technically open in the sense that flux-pro-ultra is still the SFW
  default model -- that part wasn't changed -- but the *unbounded blast
  radius* from a high-credit/scripted account is now capped regardless of
  which model gets used, which was the actually dangerous part). See
  DAILY_PHOTO_CAP in backend/app/api/routes/chat.py.

=== FINAL STATE AS OF STOPPING (2026-09-14, overnight session end) ===
Every change described in both of today's sections is committed to git
(main, HEAD = a332a6d "Implement the mobile checkout auto-login bridge")
AND deployed live on the server -- backend restarted clean each time,
frontend rebuilt and nginx restarted each time, spot-verified against the
real server after each deploy (not just "should work"). Nothing is sitting
half-finished, uncommitted, or undeployed. Safe to close for the night.

=== NEXT STEPS FOR TOMORROW (carried forward, still open) ===
1. ModelsLab NSFW image-gen subscription -- decision + purchase still
   pending (₹1,750/mo, 24h refund window if it doesn't work -- see
   research earlier in today's log).
2. Droplet upgrade decision -- still pending (current 1vCPU/512MB
   ~Rs600/mo vs ~Rs1800-2000/mo for 2vCPU/4GB).
3. flux-pro-ultra is still the default SFW image model (~Rs5/photo) --
   the daily cap added tonight bounds the blast radius but doesn't reduce
   the per-photo cost itself. Consider switching SFW default to something
   cheaper too, if margins matter more than SFW photo quality.
4. Flutter SDK still not installed/verified anywhere -- real `flutter
   analyze`/build has never been run on this codebase, including on all of
   today's mobile_app changes (WsService, ChatService, StoryScreen, the 6
   dispose() fixes, profile_screen.dart's FCM+Google signout calls). High
   value next step: get Flutter installed somewhere and actually build it.
5. Whether to gate any part of "Our Story" behind subscription tier --
   open product decision, not made yet.
6. Mobile app has still never been through a real device/emulator test run
   of anything built today -- the Story screen, the WS subprotocol switch,
   the mobile checkout bridge. All reviewed by reading code + IDE
   diagnostics only, same caveat as item 4.
